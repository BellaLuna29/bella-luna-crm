node_modules/
dist/
.vercel/
.env.local
.env
*.log
.DS_Store

# Export CSV Airtable — données personnelles clientes (santé, coordonnées),
# ne doit jamais partir dans l'historique git, même dans un repo privé.
BDD/
